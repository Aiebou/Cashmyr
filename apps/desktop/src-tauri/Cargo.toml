[package]
name = "cashmyr"
version = "0.1.2"
description = "Budget personnel hors ligne"
authors = ["Aiebou"]
license = "MIT"
repository = "https://github.com/Aiebou/Cashmyr"
edition = "2021"
publish = false

[lib]
name = "cashmyr_lib"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
# Utilisé par `tauri::generate_context!` pour la configuration des plugins (updater).
serde_json = "1"
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-process = "2"
tauri-plugin-single-instance = "2"
tauri-plugin-store = "2"
tauri-plugin-updater = "2"

[dev-dependencies]
tempfile = "3"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
strip = true
